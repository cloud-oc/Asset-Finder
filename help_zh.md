# 资源检索与核对指南

## 工具定位

Asset Finder Web 是一个面向本地项目资源的批量检索与核对工具。它可以把工程目录中的实际文件，与排期清单、上传状态表、交付记录或其他文件清单进行快速比对。

美术资产交付审查是其中一种典型场景，但它同样适合资源盘点、版本发布前检查、外包交付复核和工程目录清理。

工具只比较文件名、相对路径和扩展名，不会读取文件内容，也不会连接排期系统或上传平台。

## 推荐工作流

1. **导入实际目录**：点击顶部文件夹按钮，选择需要核对的项目目录。
2. **准备清单**：从排期表、上传状态表或交付记录中整理出文件名和相对路径。可以直接从电子表格复制两列内容。
3. **选择规则**：在资源格式筛选中选择需要检查的扩展名，并选择文件名匹配方式。
4. **批量核对**：把清单粘贴到查询框，点击“开始核对”。工具会逐条检查工程索引中是否存在对应资源。
5. **复核结果**：结果中出现的记录表示工程目录找到了对应文件；未出现在结果中的记录是需要人工进一步确认的候选项。
6. **导出记录**：完成后可以导出纯文本结果，作为审查或交付记录的辅助材料。

## 输入格式

每行一条记录，格式为：

~~~
文件名    相对/子路径
~~~

例如：

~~~
Hero_Knight    Assets/Characters
Stone_A        Assets/Environment/Rocks
~~~

文件名和路径之间可以使用多个空格或 Tab 分隔，路径可以包含多级子目录。文件名填写扩展名之前的部分，例如 `Hero_Knight` 可以匹配 `Hero_Knight.prefab` 或 `Hero_Knight.png`。

## 匹配方式

- **全字匹配**：文件名（不含扩展名）完全相等。
- **前缀匹配**：文件名以查询词开头，例如 `Stone` 可以匹配 `Stone_A`。
- **后缀匹配**：文件名以查询词结尾，例如 `_A` 可以匹配 `Stone_A`。
- **关键词匹配**：查询词出现在文件名任意位置。
- **区分大小写**：需要严格区分文件名大小写时开启。
- **忽略空格**：用于兼容清单和工程命名中不一致的空格。

如果没有选择任何资源格式，搜索不会额外限制扩展名；如果希望只检查某些类型，请在格式筛选中选择对应扩展名。

## 结果如何理解

输出会逐行列出工程索引中找到的文件名和相对目录。工具不会自动生成差异报告，也不会判断文件内容是否相同，因此以下情况仍需要人工确认：

- 清单中没有出现在结果里的记录；
- 文件名相近但并不完全匹配的记录；
- 目录层级或交付版本不符合预期的记录；
- 文件存在但内容、版本或上传状态需要进一步确认的记录。

## 大项目与取消

索引和搜索都会分块执行，并在批次之间让出浏览器线程。进度以已处理文件数 / 总文件数为主，同时显示容量、速度和预计剩余时间。

大目录导入或批量核对过程中可以点击“取消”。取消索引会保留上一次可用索引，取消搜索会保留原有结果。

## 常见问题

- **为什么导入后不能搜索？** 确认已经完成目录索引，并至少选择一种文件名匹配方式。
- **为什么结果少于清单行数？** 未出现的记录通常是路径、文件名、扩展名或版本状态需要人工复核的候选项。
- **为什么帮助内容没有显示？** 如果直接使用 `file://` 打开页面，浏览器可能禁止加载 Markdown。请使用 `python -m http.server 8000` 启动本地静态服务器。
- **支持哪些浏览器？** 文件夹选择依赖 `webkitdirectory`，推荐使用 Chrome、Edge 或其他 Chromium 浏览器。
